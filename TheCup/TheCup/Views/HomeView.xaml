<UserControl x:Class="TheCup_Presentation.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:TheCup_Presentation.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="700">

    <UserControl.Resources>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibility" />
    </UserControl.Resources>

    <UserControl.InputBindings>
        <KeyBinding Key="Enter"
                    Command="{Binding OpenTournamentCommand}" />
    </UserControl.InputBindings>

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Style="{StaticResource SectionTitle}"
                           Text="{StaticResource Home_Title}" />
                <TextBlock Style="{StaticResource SectionSubtitle}"
                           Text="{StaticResource Home_Subtitle}" />
            </StackPanel>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Top">
                <Button Style="{StaticResource DangerButton}"
                        Content="{StaticResource Home_DeleteTournament}"
                        Margin="0,0,12,0"
                        Command="{Binding DeleteTournamentCommand}" />
                <Button Style="{StaticResource SecondaryButton}"
                        Content="{StaticResource Home_OpenTournament}"
                        Margin="0,0,12,0"
                        Command="{Binding OpenTournamentCommand}" />
                <Button Style="{StaticResource SecondaryButton}"
                        Content="{StaticResource Home_Refresh}"
                        Margin="0,0,12,0"
                        Command="{Binding RefreshCommand}" />
                <Button Style="{StaticResource PrimaryButton}"
                        Content="{StaticResource Home_NewTournament}"
                        Command="{Binding NewTournamentCommand}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <ListBox Background="Transparent"
                     BorderThickness="0"
                     ItemsSource="{Binding Tournaments}"
                     SelectedItem="{Binding SelectedTournament}"
                     Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVisibility}}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,12" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <EventSetter Event="MouseDoubleClick" Handler="OnTournamentDoubleClick" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource TournamentCardBorder}"
                                Margin="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{StaticResource Brush.TextPrimary}"
                                               FontSize="18"
                                               FontWeight="SemiBold" />
                                    <TextBlock Margin="0,6,0,0"
                                               Foreground="{StaticResource Brush.TextSecondary}"
                                               FontSize="13"
                                               Text="{Binding FormattedStartDate}" />
                                    <TextBlock Margin="0,4,0,0"
                                               Foreground="{StaticResource Brush.AccentMuted}"
                                               FontSize="12"
                                               Text="{Binding SportDisplayName}" />
                                    <TextBlock Margin="0,4,0,0"
                                               Foreground="{StaticResource Brush.TextSecondary}"
                                               FontSize="12"
                                               Text="{Binding TeamCountDisplay}" />
                                    <TextBlock Margin="0,4,0,0"
                                               Foreground="{StaticResource Brush.TextSecondary}"
                                               FontSize="12"
                                               Text="{Binding PitchCountDisplay}" />
                                </StackPanel>

                                <Border Grid.Column="1"
                                        VerticalAlignment="Top"
                                        Style="{StaticResource StatusBadge}"
                                        Background="#1A2A00">
                                    <TextBlock Text="{Binding StatusDisplayName}"
                                               Foreground="{StaticResource Brush.AccentPrimary}"
                                               FontSize="12"
                                               FontWeight="SemiBold" />
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MaxWidth="420"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="{StaticResource Home_EmptyTitle}"
                           Style="{StaticResource SectionTitle}"
                           FontSize="22"
                           HorizontalAlignment="Center"
                           TextAlignment="Center" />
                <TextBlock Text="{StaticResource Home_EmptyMessage}"
                           Style="{StaticResource SectionSubtitle}"
                           TextAlignment="Center"
                           Margin="0,12,0,24" />
                <Button Style="{StaticResource PrimaryButton}"
                        Content="{StaticResource Home_NewTournament}"
                        HorizontalAlignment="Center"
                        Command="{Binding NewTournamentCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
