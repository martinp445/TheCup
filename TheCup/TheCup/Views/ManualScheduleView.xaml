<Window x:Class="TheCup_Presentation.Views.ManualScheduleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheCup_Presentation.Views"
        mc:Ignorable="d"
        Title="ManualScheduleView" Height="450" Width="800">

    <DockPanel Margin="10">

        <!-- List -->
        <ListBox ItemsSource="{Binding Items}" DockPanel.Dock="Top" Height="150">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Round}" Margin="5"/>
                        <TextBlock Text="{Binding Pitch}" Margin="5"/>
                        <TextBlock Text="{Binding TeamA}" Margin="5"/>
                        <TextBlock Text="{Binding TeamB}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Inputs -->
        <StackPanel DockPanel.Dock="Top">

            <TextBox Text="{Binding TeamA}" Margin="0,5,0,0"/>

            <TextBox Text="{Binding TeamB}" Margin="0,5,0,0"/>

            <TextBox Text="{Binding Pitch}" Margin="0,5,0,0"/>

            <TextBox Text="{Binding Round}" Margin="0,5,0,0"/>

            <Button Content="Add Item"
                    Command="{Binding AddCommand}"
                    Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Confirm -->
        <Button Content="Confirm All"
                Command="{Binding ConfirmCommand}"
                DockPanel.Dock="Bottom"
                Margin="0,10,0,0"/>

    </DockPanel>

</Window>
